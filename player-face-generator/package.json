{
  "name": "fm-face-generator",
  "version": "1.0.0",
  "description": "Football Manager-style fictional player face portrait generator",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_OPTIONS=--use-system-ca node server.js",
    "dev": "cross-env NODE_OPTIONS=--use-system-ca node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
