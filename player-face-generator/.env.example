# Which provider to use: openai | pollinations (default: openai)
IMAGE_PROVIDER=openai

# Required when IMAGE_PROVIDER=openai
OPENAI_API_KEY=sk-...

# Optional when IMAGE_PROVIDER=pollinations (raises rate limits)
# POLLINATIONS_API_KEY=

PORT=3000
